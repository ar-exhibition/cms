@model IEnumerable<cms.ar.xarchitecture.de.cmsXARCH.Term>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Term1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Term1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TermId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TermId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TermId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
